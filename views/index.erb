<article>
  <div class="button">
    <form action="/memos/new" method="get">
      <button type="submit">追加</button>
    </form>
  </div>
  <% if @memo_list.memos %>
    <% @memo_list.memos.each do |memo| %>
      <ul>
        <li><a href="/memos/<%= memo['id'] %>"><%= memo['title'] %></a></li>
      </ul>
    <% end %>
  <% end %>
</article>
